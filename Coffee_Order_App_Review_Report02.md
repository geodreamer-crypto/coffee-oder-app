# Smart Coffee Order & Inventory System  
## PRD 및 기능명세서 검토 보고서

- **검토 대상:** 커피 주문 및 재고 관리 웹 애플리케이션 PRD / 기능명세서
- **문서 버전:** v1.0
- **검토 기준일:** 2026-08-01
- **검토 관점:** 웹 애플리케이션 기획·운영·개발·데이터 정합성·보안·확장성
- **검토 목적:** 개발 착수 전 요구사항의 누락, 상충, 구현 위험 및 운영상 문제를 식별하고 수정 방향을 제안한다.

---

## 1. 총평

현재 PRD와 기능명세서는 고객 주문과 관리자 재고 관리의 기본 흐름을 비교적 명확하게 정의하고 있다.

특히 다음과 같은 핵심 사용자 흐름은 잘 정리되어 있다.

- 고객의 메뉴 조회 및 옵션 선택
- 장바구니 수량 관리
- 가상 결제 및 주문 생성
- 주문 상태 확인
- 관리자의 주문 처리
- 재고 증감 및 품절 처리
- 주문 취소 시 재고 원복

그러나 현재 문서는 정상적인 주문 흐름인 **해피패스(Happy Path)** 위주로 구성되어 있다. 실제 서비스 구현에 필요한 다음 항목은 구체성이 부족하거나 누락되어 있다.

1. 관리자 인증 및 권한 관리
2. PRD와 Prisma 데이터 모델 간 정합성
3. 동시 주문 시 재고 차감의 원자성
4. 실시간 상태 동기화 방식
5. SQLite 기반 배포 환경의 제약
6. 비로그인 고객의 주문 식별 방법
7. 중복 주문 방지
8. 메뉴 및 옵션 데이터 관리
9. 주문 상태 전이 규칙
10. 테스트 데이터 초기화에 대한 안전장치

따라서 현재 문서는 **MVP 데모 구현에는 활용할 수 있지만**, 실제 매장 운영이나 외부 사용자에게 공개하는 서비스 기준으로는 보완이 필요하다.

---

# 2. 치명적 문제 및 필수 수정사항

## 2.1 관리자 인증 및 권한 관리 누락

### 문제점

PRD와 기능명세서에 관리자 로그인 및 권한 검증 요구사항이 없다.

현재 명세만 기준으로 구현할 경우 누구나 `/admin` 경로에 접근하여 다음 작업을 수행할 수 있다.

- 메뉴 재고 변경
- 메뉴 품절 처리
- 주문 상태 변경
- 주문 취소
- 주문 데이터 삭제
- 테스트 데이터 초기화

재고 관리가 핵심 기능인 서비스에서 관리자 기능이 보호되지 않는 것은 치명적인 보안 문제다.

### 수정안

다음 기능을 신규 요구사항으로 추가한다.

#### `FR-A00` 관리자 인증 및 접근 제어

- 관리자는 인증을 완료한 경우에만 관리자 화면에 접근할 수 있다.
- 인증되지 않은 사용자가 `/admin/**` 경로에 접근하면 로그인 화면으로 이동시킨다.
- 관리자 Server Action 실행 시 화면 접근 여부와 별개로 서버에서 세션 및 권한을 다시 검증한다.
- 재고 변경, 주문 취소, 데이터 초기화 등 주요 작업은 관리자만 수행할 수 있다.
- 인증 방식은 다음 중 하나로 구현한다.
  - Auth.js 기반 관리자 계정 인증
  - Supabase Auth
  - MVP 한정 관리자 PIN 인증
- 운영 환경에서는 단순 클라이언트 상태만으로 관리자 권한을 판단하지 않는다.

### 구현 권고

- `middleware.ts`를 사용하여 `/admin/**` 경로를 보호한다.
- 모든 관리자 Server Action 내부에서 세션과 역할을 검증한다.
- 관리자 역할은 최소한 다음과 같이 정의한다.

```ts
type UserRole = "ADMIN";
```

향후 다중 매장 또는 직원 계정 기능을 고려한다면 다음과 같이 확장할 수 있다.

```ts
type UserRole = "OWNER" | "MANAGER" | "STAFF";
```

---

## 2.2 PRD와 Prisma 데이터 모델 불일치

### 문제점

PRD의 주문 데이터에는 다음 속성이 포함되어 있다.

- `option`
- `menuName`
- `unitPrice`

그러나 기존 Prisma의 `OrderItem`에는 다음 속성만 존재한다.

- `menuId`
- `quantity`
- `price`

따라서 기존 Prisma 스키마만으로는 다음 요구사항을 정상적으로 구현할 수 없다.

- HOT/ICE 옵션 저장
- 주문 당시의 메뉴명 보존
- 주문 당시의 판매 단가 보존

메뉴명이나 가격이 변경될 경우 과거 주문 내역이 현재 메뉴 정보로 표시되는 문제도 발생할 수 있다.

### 수정 원칙

주문 데이터에는 주문 생성 시점의 정보를 스냅샷으로 저장해야 한다.

- 주문 당시 메뉴명
- 주문 당시 선택 옵션
- 주문 당시 단가
- 주문 당시 옵션 추가 금액
- 주문 당시 총금액

### 수정된 Prisma 스키마

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum MenuCategory {
  COFFEE
  NON_COFFEE
  DESSERT
}

enum TemperatureOption {
  HOT
  ICE
}

enum OrderStatus {
  PENDING
  PREPARING
  COMPLETED
  PICKED_UP
  CANCELLED
}

enum PaymentMethod {
  CREDIT_CARD
  KAKAO_PAY
  CASH
}

model Menu {
  id           String      @id @default(cuid())
  name         String
  price        Int
  category     MenuCategory
  stock        Int         @default(0)
  initialStock Int         @default(0)
  imageUrl     String?
  isActive     Boolean     @default(true)
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt
  orderItems   OrderItem[]

  @@index([category, isActive])
}

model Order {
  id             String        @id @default(cuid())
  orderNo        String        @unique
  requestId      String        @unique
  status         OrderStatus   @default(PENDING)
  paymentMethod  PaymentMethod
  totalAmount    Int
  cancelledAt    DateTime?
  completedAt    DateTime?
  pickedUpAt     DateTime?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  items          OrderItem[]

  @@index([status, createdAt])
  @@index([createdAt])
}

model OrderItem {
  id              String             @id @default(cuid())
  orderId         String
  menuId          String
  menuName        String
  temperature     TemperatureOption?
  quantity        Int
  unitPrice       Int
  optionPrice     Int                @default(0)
  lineTotalAmount Int
  order           Order              @relation(fields: [orderId], references: [id], onDelete: Cascade)
  menu            Menu               @relation(fields: [menuId], references: [id], onDelete: Restrict)

  @@index([orderId])
  @@index([menuId])
}
```

### 추가 수정사항

- 내부 데이터베이스 식별자인 `id`와 고객에게 노출되는 `orderNo`를 분리한다.
- 과거 주문 참조 무결성을 위해 사용된 메뉴를 하드 삭제하지 않는다.
- 판매 중단이 필요한 메뉴는 `isActive = false`로 처리한다.
- `initialStock`을 추가하여 재고 초기화 기준값을 저장한다.
- 상태값과 카테고리값은 가능한 경우 문자열 대신 Prisma Enum을 사용한다.

---

## 2.3 주문 상태 정의 불일치

### 문제점

PRD의 주문 상태는 다음과 같다.

1. 접수 대기
2. 제조 중
3. 제조 완료
4. 수령 완료

그러나 기존 기능명세서 및 Prisma 모델에는 다음 상태만 존재한다.

- `PENDING`
- `PREPARING`
- `COMPLETED`
- `CANCELLED`

즉, PRD에 있는 `수령 완료` 상태가 데이터 모델에는 없다.

또한 관리자 기능에는 `주문 승인/제조 시작`, `제조 완료`, `주문 취소`가 정의되어 있지만 상태 간 전이 규칙이 없다.

### 수정안

주문 상태를 다음과 같이 통일한다.

| 상태 코드 | 화면 표시 | 설명 |
|---|---|---|
| `PENDING` | 접수 대기 | 주문이 생성되어 관리자 확인을 기다리는 상태 |
| `PREPARING` | 제조 중 | 관리자가 주문을 승인하고 제조를 시작한 상태 |
| `COMPLETED` | 제조 완료 | 음료 제조가 완료되어 고객 수령을 기다리는 상태 |
| `PICKED_UP` | 수령 완료 | 고객에게 상품이 전달된 상태 |
| `CANCELLED` | 주문 취소 | 주문이 취소된 상태 |

### 상태 전이 규칙

```text
PENDING → PREPARING → COMPLETED → PICKED_UP
   └──────────────→ CANCELLED
PREPARING ────────→ CANCELLED
```

### 추가 정책

- `COMPLETED` 이후 주문 취소 허용 여부를 명확하게 정해야 한다.
- v1.0에서는 다음 정책을 권장한다.
  - `PENDING`, `PREPARING`: 관리자 취소 가능
  - `COMPLETED`, `PICKED_UP`: 관리자 취소 불가
- 이미 `CANCELLED`인 주문을 다시 취소할 수 없어야 한다.
- 이전 단계로 상태를 되돌리는 기능은 v1.0에서 지원하지 않는다.
- 상태 변경 가능 여부는 서버에서 검증한다.

---

## 2.4 재고 동시성 처리 방식 불충분

### 문제점

기존 문서는 “DB 트랜잭션으로 재고 차감”이라고만 정의하고 있다.

단순히 재고를 조회한 후 차감하는 방식은 다음과 같은 경쟁 상태를 막지 못한다.

```text
사용자 A 재고 조회: 1
사용자 B 재고 조회: 1
사용자 A 주문 성공: 재고 0
사용자 B 주문 성공: 재고 -1
```

결제 직전 재고를 다시 조회하더라도 조회와 차감 사이에 다른 주문이 발생할 수 있다.

### 수정안

주문 생성과 재고 차감은 반드시 하나의 DB 트랜잭션 안에서 실행한다.

재고 차감은 다음 조건을 만족하는 경우에만 수행해야 한다.

```text
현재 재고 >= 주문 수량
```

### 구현 예시

```ts
await prisma.$transaction(async (tx) => {
  for (const item of cartItems) {
    const result = await tx.menu.updateMany({
      where: {
        id: item.menuId,
        isActive: true,
        stock: {
          gte: item.quantity,
        },
      },
      data: {
        stock: {
          decrement: item.quantity,
        },
      },
    });

    if (result.count === 0) {
      throw new Error(`STOCK_NOT_ENOUGH:${item.menuId}`);
    }
  }

  await tx.order.create({
    data: {
      orderNo,
      requestId,
      paymentMethod,
      totalAmount,
      items: {
        create: normalizedItems,
      },
    },
  });
});
```

### 필수 검증사항

서버는 클라이언트가 전달한 다음 값을 신뢰해서는 안 된다.

- 메뉴명
- 단가
- 총 결제 금액
- 현재 재고
- 옵션 가격

서버에서 메뉴 데이터를 다시 조회한 후 다음 값을 재계산해야 한다.

```text
항목 금액 = (메뉴 기본 가격 + 옵션 추가 가격) × 수량
총 결제 금액 = 모든 항목 금액의 합
```

---

## 2.5 주문 취소와 재고 원복의 원자성

### 문제점

주문 취소 시 주문 상태 변경과 재고 원복이 서로 다른 요청이나 트랜잭션에서 실행되면 다음 문제가 발생할 수 있다.

- 주문은 취소됐지만 재고가 복구되지 않음
- 재고는 복구됐지만 주문이 취소되지 않음
- 취소 버튼 중복 클릭으로 재고가 두 번 복구됨

### 수정안

주문 상태 변경과 재고 원복을 동일한 DB 트랜잭션에서 처리한다.

### 처리 조건

- 현재 주문 상태가 취소 가능한 상태인지 확인한다.
- `CANCELLED` 상태인 주문은 다시 취소할 수 없다.
- 주문 상태 변경과 모든 메뉴 재고 원복이 함께 성공해야 한다.
- 하나라도 실패하면 전체 작업을 롤백한다.

### 구현 예시

```ts
await prisma.$transaction(async (tx) => {
  const order = await tx.order.findUnique({
    where: { id: orderId },
    include: { items: true },
  });

  if (!order) {
    throw new Error("ORDER_NOT_FOUND");
  }

  if (!["PENDING", "PREPARING"].includes(order.status)) {
    throw new Error("ORDER_CANNOT_BE_CANCELLED");
  }

  const updated = await tx.order.updateMany({
    where: {
      id: orderId,
      status: {
        in: ["PENDING", "PREPARING"],
      },
    },
    data: {
      status: "CANCELLED",
      cancelledAt: new Date(),
    },
  });

  if (updated.count === 0) {
    throw new Error("ORDER_ALREADY_CHANGED");
  }

  for (const item of order.items) {
    await tx.menu.update({
      where: { id: item.menuId },
      data: {
        stock: {
          increment: item.quantity,
        },
      },
    });
  }
});
```

---

## 2.6 실시간 동기화 요구사항과 구현 방식 불일치

### 문제점

비기능 요구사항에는 다음 내용이 있다.

> 사용자 화면의 주문 및 관리자 화면의 재고 수정이 상호 즉시 반영되어야 한다.

그러나 기능명세서에서는 다음 방식이 혼재되어 있다.

- `revalidatePath()`
- Server Action
- Polling
- SWR

`revalidatePath()`는 서버 캐시를 무효화하는 기능이며, 이미 열린 다른 사용자의 브라우저 화면을 자동 갱신하는 실시간 Push 기능이 아니다.

### 수정안

v1.0에서 실시간성 수준을 명확하게 정의한다.

#### 권장 MVP 방식

- 메뉴 및 재고: 5초 간격 Polling
- 고객 주문 상태: 3초 간격 Polling
- 관리자 주문 대기열: 3초 간격 Polling
- 관리자 재고 수정 완료 후 즉시 `mutate()`를 호출하여 화면 갱신
- 결제 시 서버에서 최종 재고 검증

#### 향후 확장 방식

다음 중 하나를 활용해 실시간 Push 방식으로 전환할 수 있다.

- Supabase Realtime
- Server-Sent Events
- Pusher
- Ably
- WebSocket

### 수정된 비기능 요구사항

> v1.0의 “실시간”은 서버 상태 변경 후 최대 5초 이내에 다른 클라이언트 화면에 반영되는 준실시간(Near Real-Time)을 의미한다. 주문 생성 및 재고 변경의 데이터 정합성은 Polling 여부와 관계없이 서버 트랜잭션으로 보장한다.

---

## 2.7 SQLite와 서버리스 배포 환경의 부조합

### 문제점

기술스택에서 SQLite를 기본 데이터베이스로 제시하고 있으나, Vercel과 같은 서버리스 환경에서는 로컬 SQLite 파일을 운영 데이터베이스로 사용하는 것이 적절하지 않다.

발생 가능한 문제는 다음과 같다.

- 서버 인스턴스마다 파일이 분리될 수 있음
- 배포 후 데이터가 유지되지 않을 수 있음
- 파일시스템 쓰기 제한
- 동시 쓰기 성능 및 잠금 문제
- 실시간 재고 관리에 부적합

### 수정안

환경별 데이터베이스 정책을 분리한다.

| 환경 | 데이터베이스 |
|---|---|
| 로컬 개발 | SQLite 또는 로컬 PostgreSQL |
| 테스트 | PostgreSQL 테스트 DB |
| 스테이징 | Supabase PostgreSQL 또는 Neon |
| 운영 | Supabase PostgreSQL, Neon 또는 관리형 PostgreSQL |

### 기술스택 수정 문구

> SQLite는 로컬 개발과 프로토타입 용도로만 사용한다. 운영 배포 환경에서는 동시성, 데이터 지속성 및 서버리스 호환성을 위해 PostgreSQL을 사용한다.

---

# 3. 주요 개선사항

## 3.1 옵션 모델과 가격 정책 미정의

### 문제점

현재 옵션은 HOT/ICE만 정의되어 있지만 다음 사항이 명확하지 않다.

- HOT/ICE 선택이 모든 메뉴에 적용되는지
- 디저트에도 옵션 모달을 표시하는지
- 옵션별 추가 금액이 있는지
- 메뉴별 허용 옵션이 다른지
- 옵션을 반드시 선택해야 하는지

### v1.0 최소 정책안

- 커피 및 논커피 메뉴 중 온도 선택 가능 메뉴에만 HOT/ICE 옵션을 제공한다.
- 디저트에는 온도 옵션을 제공하지 않는다.
- v1.0에서는 HOT/ICE에 따른 가격 차이가 없다.
- 메뉴별로 허용 가능한 온도 옵션을 설정한다.
- 옵션 선택이 필요한 메뉴는 기본 옵션을 임의로 자동 선택하지 않는다.

### 확장 모델 권고

샷 추가, 사이즈 변경, 시럽 추가 등을 지원하려면 별도 옵션 모델이 필요하다.

```prisma
model MenuOption {
  id        String  @id @default(cuid())
  menuId    String
  name      String
  value     String
  priceDiff Int     @default(0)
  isActive  Boolean @default(true)
  menu      Menu    @relation(fields: [menuId], references: [id], onDelete: Cascade)

  @@index([menuId, isActive])
}
```

---

## 3.2 관리자 메뉴 CRUD 기능 누락

### 문제점

현재 관리자 기능은 재고 수정만 지원한다.

따라서 신규 메뉴를 추가하거나 기존 메뉴의 가격, 카테고리, 이미지 등을 변경할 수 없다.

### 신규 요구사항

#### `FR-A05` 메뉴 관리

- 관리자는 메뉴를 등록할 수 있다.
- 메뉴명, 가격, 카테고리, 이미지, 초기 재고를 수정할 수 있다.
- 판매 중단 메뉴는 비활성화할 수 있다.
- 기존 주문에서 사용된 메뉴는 하드 삭제하지 않는다.
- 메뉴 비활성화 시 고객 화면에서 즉시 숨김 처리한다.
- 메뉴 가격 변경은 신규 주문부터 적용되며 기존 주문 금액에는 영향을 주지 않는다.

---

## 3.3 비로그인 고객의 주문 식별 방식 누락

### 문제점

고객 로그인 기능이 정의되지 않았지만 “내 주문 상태 확인” 기능이 존재한다.

현재 명세만으로는 다음 상황에서 주문을 다시 찾을 수 없다.

- 주문 완료 후 브라우저를 닫은 경우
- 주문 완료 페이지에서 이탈한 경우
- 새로고침한 경우
- 다른 기기에서 확인하려는 경우

### 수정안

v1.0에서는 비회원 주문 방식으로 한정한다.

- 주문 성공 시 `orderNo`를 생성한다.
- `/order/[orderNo]` 페이지로 이동한다.
- 최근 주문 번호를 `localStorage`에 저장한다.
- 고객 메인 화면에 최근 주문 바로가기를 표시한다.
- 주문 번호는 추측하기 어렵게 구성하거나 별도 조회 토큰을 사용한다.

### 보안 권고

단순 증가 번호만 URL에 사용하는 경우 다른 사용자의 주문을 추측할 수 있다.

다음 중 하나를 권장한다.

1. 주문 번호와 조회 토큰을 함께 사용
2. 충분히 긴 랜덤 공개 식별자 사용
3. 고객에게 보여주는 짧은 주문 번호와 URL용 토큰 분리

예시:

```json
{
  "orderNo": "A-102",
  "publicToken": "pk_8F3mZpL2sR7x"
}
```

---

## 3.4 중복 주문 방지 누락

### 문제점

결제 버튼을 여러 번 누르거나 네트워크 요청이 재시도되면 동일 주문이 중복 생성될 수 있다.

이 경우 다음 문제가 발생한다.

- 동일 주문 여러 건 생성
- 재고 중복 차감
- 결제 금액 중복 처리
- 고객 문의 발생

### 수정안

주문 생성 요청에 멱등성 키를 적용한다.

#### 처리 방식

1. 장바구니 제출 시 클라이언트가 고유한 `requestId`를 생성한다.
2. 서버는 `requestId`를 주문과 함께 저장한다.
3. 동일한 `requestId`가 다시 전달되면 신규 주문을 생성하지 않는다.
4. 처리 중에는 결제 버튼을 비활성화한다.

```ts
const requestId = crypto.randomUUID();
```

Prisma 모델에는 다음 제약을 둔다.

```prisma
requestId String @unique
```

---

## 3.5 관리자 신규 주문 알림 부족

### 문제점

관리자가 주문 목록 화면을 보고 있지 않거나 새 주문 유입을 인지하지 못할 수 있다.

### 수정안

#### `FR-A06` 신규 주문 알림

- 신규 주문 발생 시 관리자 화면에 Toast를 표시한다.
- 접수 대기 주문 수를 배지로 표시한다.
- 사용자 상호작용 후 알림음 재생을 활성화할 수 있다.
- 동일 주문에 대해 알림이 반복되지 않도록 마지막 확인 주문 시점을 관리한다.
- 브라우저 알림은 v1.1 백로그로 분리할 수 있다.

---

## 3.6 품절 임박 경고 기준 부족

### 문제점

재고가 0인 경우만 품절 처리한다. 관리자가 품절 발생 전에 재고 부족을 확인하기 어렵다.

### 수정안

- 기본 품절 임박 기준을 3개로 설정한다.
- 재고가 임계값 이하이면 관리자 화면에 경고 색상을 표시한다.
- 재고가 0이면 품절 상태로 표시한다.
- 향후 메뉴별 `lowStockThreshold` 설정 기능을 지원할 수 있다.

```prisma
lowStockThreshold Int @default(3)
```

v1.0 범위가 과도해질 경우 메뉴별 임계값은 v1.1로 분리하고, 공통 임계값 3개를 적용한다.

---

# 4. 세부 보완사항

| 항목 | 현재 문제 | 수정 방향 |
|---|---|---|
| 시간대 | UTC 저장 예시는 있으나 화면 표시 정책이 없음 | DB에는 UTC로 저장하고 화면에서는 `Asia/Seoul` 기준으로 표시 |
| 가상 결제 | 실제 PG 연동 여부가 불명확 | 실제 결제 승인 없이 성공을 모사하는 Mock 결제임을 명시 |
| 결제 수단 | 현금 주문의 운영 흐름이 정의되지 않음 | v1.0에서는 선택값만 저장하며 실제 정산은 지원하지 않음을 명시 |
| 이미지 | 로컬 `/images` 경로만 제시됨 | 정적 이미지 또는 외부 Object Storage 사용 정책 명시 |
| 주문 취소 | 전체 취소만 정의됨 | 항목 단위 부분 취소는 v1.0에서 제외한다고 명시 |
| 데이터 초기화 | 재고와 주문 삭제가 한 기능으로 묶여 있음 | 재고 초기화와 주문 내역 삭제를 분리 |
| 초기화 보안 | 운영 환경에서 실행될 위험 | 개발·스테이징에서만 노출하거나 환경변수로 비활성화 |
| 재고 음수 | 관리자 직접 입력 시 음수 가능성 | 입력 최소값을 0으로 제한하고 서버에서도 검증 |
| 가격 | 음수 또는 0원 입력 가능성 | 가격은 0 이상 정수로 검증 |
| 수량 | 소수 입력 가능성 | 재고와 주문 수량은 양의 정수로 제한 |
| 메뉴 비활성화 | 품절과 판매 중단의 구분이 없음 | `stock = 0`은 품절, `isActive = false`는 판매 중단으로 구분 |
| 에러 메시지 | 품절 메시지만 정의됨 | 주문 없음, 네트워크 오류, 상태 변경 충돌 등 표준 오류 정의 |
| 모바일 UX | 모달/드로어 사용 기준이 혼재됨 | 모바일은 바텀시트, 데스크톱은 Dialog 사용 권장 |
| 접근성 | 키보드, 스크린리더 기준 없음 | Dialog focus trap, 버튼 aria-label, 명도 대비 요구 추가 |
| 로딩 상태 | 요청 중 UI 정의 없음 | Skeleton, 버튼 로딩, 중복 클릭 방지 상태 추가 |

---

# 5. 수정된 기능 요구사항

## 5.1 고객 화면

### `FR-C01` 메뉴 목록 조회 및 필터링

- 카테고리별 메뉴 필터링 기능을 제공한다.
- 활성화된 메뉴만 고객 화면에 노출한다.
- 각 메뉴 카드에는 다음 정보를 표시한다.
  - 이미지
  - 메뉴명
  - 기본 가격
  - 남은 재고 수량
- 재고가 0인 메뉴는 품절 배지를 표시하고 선택을 비활성화한다.
- 메뉴 데이터는 최대 5초 간격으로 갱신한다.
- 메뉴 카드 선택 시 서버에서 조회한 최신 메뉴 정보를 기준으로 옵션 화면을 표시한다.

### `FR-C02` 메뉴 옵션 선택

- 메뉴에 설정된 옵션만 표시한다.
- 온도 옵션 대상 메뉴는 HOT 또는 ICE를 선택할 수 있다.
- 디저트 등 온도 옵션 비대상 메뉴에는 해당 선택지를 표시하지 않는다.
- 수량은 1 이상 현재 재고 이하의 정수만 허용한다.
- 수량이 현재 재고를 초과하면 추가 버튼을 비활성화하고 안내 메시지를 표시한다.
- v1.0에서는 HOT/ICE 옵션에 따른 추가 가격이 없다.

### `FR-C03` 장바구니 관리

- 장바구니에 메뉴명, 옵션, 수량, 단가, 항목 금액을 표시한다.
- 동일한 메뉴와 동일한 옵션 조합은 하나의 항목으로 합산한다.
- 동일 메뉴라도 옵션이 다르면 별도 항목으로 관리한다.
- 장바구니에서 수량을 변경하거나 항목을 삭제할 수 있다.
- 전체 삭제 기능을 제공한다.
- 수량 증가 시 최신 재고 정보를 기준으로 검증한다.
- 장바구니 데이터는 선택적으로 `localStorage`에 저장한다.
- 장바구니에 저장된 가격과 재고는 참고 정보이며 최종 주문 시 서버가 다시 검증한다.

### `FR-C04` 주문 및 가상 결제

- 고객은 결제 수단을 선택할 수 있다.
- v1.0의 결제는 실제 PG 연동이 없는 Mock 결제로 한정한다.
- 주문 생성 전 서버에서 다음 항목을 검증한다.
  - 메뉴 존재 여부
  - 메뉴 활성화 여부
  - 현재 재고
  - 옵션 유효성
  - 서버 기준 단가
  - 총 결제 금액
- 재고 차감과 주문 생성은 하나의 DB 트랜잭션에서 처리한다.
- 주문 요청에는 `requestId`를 포함해 중복 주문을 방지한다.
- 주문 성공 시 주문 번호와 조회용 공개 토큰을 반환한다.
- 주문 실패 시 장바구니를 자동으로 비우지 않는다.

### `FR-C05` 주문 상태 조회

- 주문 완료 후 주문 상태 페이지로 이동한다.
- 상태 페이지에는 다음 정보를 표시한다.
  - 주문 번호
  - 주문 시각
  - 주문 메뉴
  - 총금액
  - 결제 수단
  - 현재 상태
- 주문 상태는 최대 3초 간격으로 갱신한다.
- 주문 상태 단계는 다음과 같다.
  - 접수 대기
  - 제조 중
  - 제조 완료
  - 수령 완료
- 취소 주문은 별도 취소 상태로 표시한다.
- 최근 주문 조회 정보는 브라우저에 저장할 수 있다.

---

## 5.2 관리자 화면

### `FR-A00` 관리자 인증 및 접근 제어

- 관리자 화면은 인증된 관리자만 접근할 수 있다.
- 관리자 Server Action은 매 요청마다 권한을 검증한다.
- 인증되지 않은 접근은 관리자 로그인 화면으로 이동시킨다.

### `FR-A01` 실시간 재고 관리

- 전체 활성 메뉴와 비활성 메뉴의 재고를 조회할 수 있다.
- 메뉴별 재고를 `+`, `-` 버튼으로 변경할 수 있다.
- 재고를 숫자로 직접 입력할 수 있다.
- 재고는 0 이상의 정수만 허용한다.
- 재고가 0이 되면 고객 화면에서 품절 처리한다.
- 재고 부족 기준 이하인 메뉴는 경고 표시한다.
- 재고 변경 후 관리자 화면은 즉시 갱신한다.
- 고객 화면은 최대 5초 이내 변경사항을 반영한다.

### `FR-A02` 주문 대기열 및 상태 관리

- 주문을 최신순으로 조회한다.
- 주문 카드에는 다음 정보를 표시한다.
  - 주문 번호
  - 주문 시각
  - 주문 메뉴 및 옵션
  - 수량
  - 총금액
  - 결제 수단
  - 현재 상태
- 관리자에게 현재 상태에서 가능한 다음 작업만 표시한다.
- 상태 전이 규칙은 다음과 같다.

```text
PENDING → PREPARING → COMPLETED → PICKED_UP
```

- 신규 주문 발생 시 Toast와 대기 주문 수 배지를 표시한다.

### `FR-A03` 주문 취소 및 재고 원복

- `PENDING`과 `PREPARING` 주문만 취소할 수 있다.
- 주문 취소 전 확인 Dialog를 표시한다.
- 주문 취소와 재고 원복은 동일한 DB 트랜잭션으로 처리한다.
- 이미 취소된 주문의 재고는 다시 원복하지 않는다.
- v1.0에서는 주문 전체 취소만 지원한다.
- 항목별 부분 취소는 지원하지 않는다.

### `FR-A04` 테스트 데이터 초기화

- 재고 초기화와 주문 내역 삭제를 별도 기능으로 제공한다.
- 초기화 실행 전 확인 문구 입력을 요구한다.
- 운영 환경에서는 초기화 기능을 비활성화한다.
- 재고 초기화 시 `initialStock` 값으로 복원한다.
- 주문 내역 삭제는 개발 또는 테스트 환경에서만 허용한다.

### `FR-A05` 메뉴 관리

- 메뉴를 등록하고 수정할 수 있다.
- 메뉴명, 가격, 카테고리, 이미지, 초기 재고를 관리할 수 있다.
- 메뉴를 판매 중단 또는 판매 재개할 수 있다.
- 주문 이력이 있는 메뉴는 하드 삭제하지 않는다.
- 가격 변경은 신규 주문부터 적용한다.

### `FR-A06` 신규 주문 알림

- 신규 주문 발생 시 관리자 화면에 Toast를 표시한다.
- 접수 대기 주문 수를 배지로 표시한다.
- 설정에 따라 알림음을 재생한다.
- 동일 주문에 대한 중복 알림을 방지한다.

---

# 6. 수정된 예외 처리 시나리오

## 6.1 결제 직전 재고 부족

### 상황

고객이 장바구니에 상품을 담은 후 다른 고객이 먼저 결제를 완료하거나 관리자가 재고를 줄였다.

### 처리

- 서버에서 최신 재고를 검증한다.
- 재고가 부족한 항목이 하나라도 있으면 전체 주문을 생성하지 않는다.
- 재고 차감도 전부 롤백한다.
- 부족한 메뉴명과 현재 주문 가능 수량을 안내한다.
- 장바구니는 유지한다.

---

## 6.2 동시에 마지막 재고 주문

### 상황

두 명 이상의 고객이 마지막 재고를 동시에 주문한다.

### 처리

- 조건부 재고 차감과 DB 트랜잭션을 사용한다.
- 먼저 원자적 차감에 성공한 요청만 주문을 생성한다.
- 나머지 요청에는 재고 부족 오류를 반환한다.
- 재고가 음수가 되는 것을 허용하지 않는다.

---

## 6.3 중복 결제 요청

### 상황

고객이 결제 버튼을 연속 클릭하거나 브라우저가 요청을 재전송한다.

### 처리

- 요청마다 고유한 `requestId`를 사용한다.
- 동일 `requestId`로는 주문을 한 건만 생성한다.
- 요청 처리 중 결제 버튼을 비활성화한다.

---

## 6.4 주문 취소 중복 실행

### 상황

관리자가 주문 취소 버튼을 여러 번 클릭하거나 여러 관리자 화면에서 동시에 취소한다.

### 처리

- 취소 가능한 현재 상태를 조건으로 원자적 상태 변경을 수행한다.
- 최초 취소 요청만 성공한다.
- 최초 요청에서만 재고를 원복한다.
- 이후 요청에는 이미 처리된 주문이라는 메시지를 표시한다.

---

## 6.5 장바구니 메뉴 비활성화

### 상황

고객이 장바구니에 담은 메뉴를 관리자가 판매 중단 처리한다.

### 처리

- 주문 생성 시 `isActive` 값을 검증한다.
- 비활성화된 메뉴가 있으면 주문을 차단한다.
- 해당 항목을 장바구니에서 제거하도록 안내한다.

---

## 6.6 네트워크 오류

### 상황

주문 요청 후 응답을 받기 전에 네트워크가 끊긴다.

### 처리

- 고객이 동일 `requestId`로 주문 결과를 다시 확인할 수 있게 한다.
- 단순히 결제를 다시 요청하도록 안내하지 않는다.
- 서버에 주문이 생성되었는지 조회한 후 결과를 표시한다.

---

## 6.7 관리자 재고 입력 오류

### 상황

관리자가 음수, 소수, 문자 또는 지나치게 큰 값을 입력한다.

### 처리

- 클라이언트에서 0 이상의 정수만 입력하도록 제한한다.
- 서버에서도 동일 규칙으로 다시 검증한다.
- 시스템 최대 재고값을 정의할 경우 해당 범위를 초과할 수 없다.

---

# 7. 수정된 비기능 요구사항

## 7.1 반응형 레이아웃

- 고객 화면은 최소 375px 모바일 화면부터 지원한다.
- 관리자 화면은 최소 768px 태블릿 및 데스크톱 화면에 최적화한다.
- 고객 옵션 선택 및 장바구니는 모바일에서 바텀시트 형태를 우선 적용한다.
- 관리자 테이블은 작은 화면에서 가로 스크롤 또는 카드 레이아웃을 제공한다.

## 7.2 성능

- 메뉴 목록의 초기 콘텐츠는 서버에서 렌더링한다.
- 이미지 최적화를 위해 `next/image`를 사용한다.
- 고객 메뉴 갱신 주기는 기본 5초로 설정한다.
- 주문 상태와 관리자 주문 대기열 갱신 주기는 기본 3초로 설정한다.
- 반복 조회 API는 필요한 필드만 반환한다.

## 7.3 데이터 정합성

- 주문 생성과 재고 차감은 하나의 트랜잭션으로 처리한다.
- 주문 취소와 재고 원복은 하나의 트랜잭션으로 처리한다.
- 재고는 0 미만이 될 수 없다.
- 주문 금액은 서버에서 계산한다.
- 과거 주문의 메뉴명과 가격은 주문 시점의 스냅샷을 사용한다.

## 7.4 보안

- 관리자 경로 및 Server Action은 인증과 권한 검증을 수행한다.
- 클라이언트가 전달한 가격, 총금액, 재고를 신뢰하지 않는다.
- 입력값은 서버에서 검증한다.
- 관리자 데이터 초기화 기능은 운영 환경에서 비활성화한다.
- 주문 조회용 공개 식별자는 단순 순차 번호만 사용하지 않는다.

## 7.5 접근성

- 모든 버튼에 식별 가능한 텍스트 또는 `aria-label`을 제공한다.
- Dialog와 바텀시트는 키보드 포커스를 관리한다.
- 품절, 경고, 상태 표시는 색상만으로 구분하지 않는다.
- 텍스트와 배경의 명도 대비를 WCAG 기준에 맞춘다.
- Toast 외에도 화면 내 오류 메시지를 제공한다.

## 7.6 오류 처리

- 사용자에게 내부 DB 오류나 스택 트레이스를 노출하지 않는다.
- 서버 오류는 표준 오류 코드로 반환한다.
- 주요 오류 코드는 다음과 같이 정의할 수 있다.

```ts
type ErrorCode =
  | "UNAUTHORIZED"
  | "FORBIDDEN"
  | "MENU_NOT_FOUND"
  | "MENU_NOT_ACTIVE"
  | "INVALID_OPTION"
  | "INVALID_QUANTITY"
  | "STOCK_NOT_ENOUGH"
  | "ORDER_NOT_FOUND"
  | "ORDER_ALREADY_EXISTS"
  | "INVALID_ORDER_STATUS"
  | "ORDER_CANNOT_BE_CANCELLED"
  | "INTERNAL_SERVER_ERROR";
```

## 7.7 관측성 및 감사 로그

- 서버 오류는 운영 로그에 기록한다.
- 주문 생성, 주문 취소, 재고 변경은 추적 가능한 형태로 기록하는 것을 권장한다.
- 관리자 계정이 여러 명인 경우 변경 주체와 변경 시각을 저장해야 한다.
- v1.0에서 감사 로그를 제외할 경우 v1.1 필수 백로그로 등록한다.

---

# 8. 기술스택 수정안

| 구분 | 수정 기술 / 라이브러리 | 역할 |
|---|---|---|
| Framework | Next.js App Router | 서버 렌더링, Route Handler, Server Action |
| Language | TypeScript | 타입 안정성 |
| Styling | Tailwind CSS + Shadcn UI | 반응형 UI 및 공통 컴포넌트 |
| Client State | Zustand | 장바구니 상태 |
| Server State | SWR 또는 TanStack Query | 메뉴, 재고, 주문 상태 Polling 및 캐시 |
| Database | PostgreSQL | 운영 데이터 저장 및 동시성 처리 |
| ORM | Prisma | 데이터 모델 및 트랜잭션 |
| Authentication | Auth.js 또는 Supabase Auth | 관리자 인증 |
| Validation | Zod | 요청 데이터 검증 |
| Icons | Lucide React | 아이콘 |
| Toast | Sonner | 사용자 알림 |
| Realtime | v1.0 Polling, 향후 Supabase Realtime/SSE | 상태 동기화 |
| Testing | Vitest + Playwright | 단위 및 E2E 테스트 |

---

# 9. 권장 디렉터리 구조

```text
src/
├── app/
│   ├── (customer)/
│   │   ├── page.tsx
│   │   └── order/
│   │       └── [publicToken]/
│   │           └── page.tsx
│   ├── admin/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── inventory/
│   │   │   └── page.tsx
│   │   └── menus/
│   │       └── page.tsx
│   ├── api/
│   │   ├── menus/
│   │   │   └── route.ts
│   │   ├── orders/
│   │   │   └── [publicToken]/
│   │   │       └── route.ts
│   │   └── admin/
│   │       └── orders/
│   │           └── route.ts
│   └── layout.tsx
├── actions/
│   ├── menu-actions.ts
│   ├── inventory-actions.ts
│   ├── order-actions.ts
│   └── reset-actions.ts
├── components/
│   ├── customer/
│   ├── admin/
│   └── ui/
├── store/
│   └── use-cart-store.ts
├── lib/
│   ├── auth.ts
│   ├── prisma.ts
│   ├── errors.ts
│   ├── order-number.ts
│   └── validators/
│       ├── menu.ts
│       ├── order.ts
│       └── inventory.ts
├── types/
│   ├── menu.ts
│   └── order.ts
└── middleware.ts
```

---

# 10. 개발 우선순위 제안

## P0 — 개발 착수 전 확정 필수

- 관리자 인증 및 권한 검증
- 주문 상태 코드 통일
- `OrderItem` 옵션 및 가격 스냅샷 추가
- 운영 데이터베이스 PostgreSQL 확정
- 주문 생성과 재고 차감 트랜잭션
- 주문 취소와 재고 원복 트랜잭션
- 서버 기준 가격 재계산
- 중복 주문 방지
- 비회원 주문 조회 방식

## P1 — v1.0 출시 전 필수

- 메뉴 및 재고 Polling
- 관리자 주문 대기열 갱신
- 메뉴 등록·수정·비활성화
- 데이터 초기화 환경 제한
- 표준 오류 코드 및 사용자 메시지
- 주문 상태 전이 검증
- 네트워크 오류 복구
- 반응형 UI와 기본 접근성
- 핵심 주문 시나리오 E2E 테스트

## P2 — v1.1 개선 권장

- Supabase Realtime 또는 SSE
- 관리자 변경 감사 로그
- 품절 임박 임계값 설정
- 브라우저 알림
- 옵션 그룹 및 추가 가격
- 항목별 부분 취소
- 고객 회원가입 및 주문 이력
- 다중 매장 지원
- 실제 PG 결제 연동

---

# 11. 핵심 테스트 시나리오

## 주문 및 재고

1. 재고 5개인 메뉴를 2개 주문하면 재고가 3개가 된다.
2. 재고보다 많은 수량을 주문하면 주문이 생성되지 않는다.
3. 두 고객이 마지막 재고를 동시에 주문하면 한 건만 성공한다.
4. 클라이언트가 가격을 변조해도 서버 가격으로 주문된다.
5. 비활성화된 메뉴는 주문할 수 없다.
6. 동일 `requestId` 요청은 주문을 한 건만 생성한다.

## 주문 취소

1. `PENDING` 주문 취소 시 재고가 정확히 원복된다.
2. `PREPARING` 주문 취소 시 재고가 정확히 원복된다.
3. 동일 주문을 두 번 취소해도 재고는 한 번만 원복된다.
4. `COMPLETED` 또는 `PICKED_UP` 주문은 정책에 따라 취소할 수 없다.
5. 재고 원복 중 오류가 발생하면 주문 상태도 변경되지 않는다.

## 관리자 보안

1. 비로그인 사용자는 `/admin`에 접근할 수 없다.
2. 비로그인 사용자는 관리자 Server Action을 실행할 수 없다.
3. 운영 환경에서는 데이터 초기화 기능이 노출되지 않는다.
4. 잘못된 재고 입력은 서버에서 거부된다.

## 고객 주문 조회

1. 주문 성공 후 주문 상태 페이지로 이동한다.
2. 유효하지 않은 공개 토큰은 주문 정보를 노출하지 않는다.
3. 주문 상태 변경 후 최대 3초 이내 고객 화면에 반영된다.
4. 주문이 취소되면 취소 상태와 안내 메시지를 표시한다.

---

# 12. 최종 평가

| 평가 항목 | 기존 문서 상태 | 수정 필요도 |
|---|---|---|
| 고객 주문 흐름 | 기본 흐름 정의됨 | 보완 필요 |
| 관리자 운영 흐름 | 재고 및 주문 관리 중심으로 정의됨 | 인증·메뉴 관리 추가 필요 |
| 데이터 모델 | PRD와 Prisma 간 불일치 | 필수 수정 |
| 주문 상태 | 수령 완료 상태 누락 | 필수 수정 |
| 동시성 | 개념만 언급됨 | 필수 수정 |
| 재고 원복 | 기능은 있으나 중복 처리 방지 미정의 | 필수 수정 |
| 실시간성 | 요구사항과 구현 방식이 불명확 | 기준 재정의 필요 |
| 보안 | 관리자 인증 누락 | 필수 수정 |
| 배포 아키텍처 | SQLite 운영 사용 위험 | 필수 수정 |
| 결제 | 실제 결제와 Mock 결제 범위 불명확 | 명문화 필요 |
| 확장성 | 옵션, 메뉴 관리, 감사 로그 부족 | 단계적 개선 필요 |

---

# 13. 결론

현재 PRD와 기능명세서는 MVP의 사용자 흐름을 설명하는 출발점으로는 적절하다. 그러나 개발 착수 전에 다음 사항을 반드시 확정해야 한다.

1. 관리자 인증과 서버 권한 검증
2. PRD와 Prisma 모델 간 필드 일치
3. `PICKED_UP`을 포함한 주문 상태 통일
4. 조건부 재고 차감과 DB 트랜잭션
5. 주문 취소 시 재고 원복의 멱등성
6. 운영 환경의 PostgreSQL 사용
7. 준실시간 갱신 주기와 구현 방식
8. 비회원 주문 식별 및 조회 정책
9. 중복 주문 방지를 위한 `requestId`
10. 실제 결제 미연동 범위 명시

특히 **관리자 인증**, **재고 동시성**, **취소 시 재고 원복**, **주문 데이터 스냅샷**은 서비스의 보안과 데이터 정합성에 직접 영향을 주므로 개발 편의를 이유로 생략해서는 안 된다.

위 수정사항을 반영하면 해당 문서는 단순 화면 구현 명세를 넘어 실제 개발, 테스트 및 운영 기준으로 사용할 수 있는 수준의 요구사항 문서가 된다.