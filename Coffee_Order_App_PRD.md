# [PRD] 커피 주문 및 재고 관리 웹 애플리케이션 (v1.0)

## 1. 문서 및 프로젝트 개요

* **프로젝트명:** Smart Coffee Order & Inventory System
* **작성 목적:** 사용자 중심의 매장 음료 주문 기능과 관리자 중심의 실시간 재고 및 매장 운영 관리 시스템 구현
* **비즈니스 특수성 고려:** 오프라인 카페 비즈니스의 환경적 특성(주방 태블릿 활용, 브라우저 이탈, 실시간 주문 인입 알림) 및 웹 서비스의 보안·동시성·정합성 요구사항을 완충하는 실무 수준의 명세 제공
* **주요 타겟:**
  * **고객(Customer):** 모바일/웹 환경에서 별도 회원가입 없이 간편하게 메뉴를 선택하고 가상 결제 및 실시간 제조 상태를 확인하려는 사용자 (비회원 Guest 기반)
  * **관리자(Admin):** 인증된 세션을 통해 매장의 영업 상태를 제어하고, 실시간 신규 주문 수주(음성 알림) 파악 및 재고/메뉴 정보를 제어하려는 점주/매니저

---

## 2. 유저 퍼소나 & 유스케이스

| 구분 | 주요 사용 목적 | 핵심 유스케이스 |
| :--- | :--- | :--- |
| **Customer<br>(Guest)** | 대기 시간 최소화 및<br>간편한 비회원 주문 | • 매장 영업 중 확인 $\rightarrow$ 메뉴 목록/품절 확인 $\rightarrow$ 옵션(HOT/ICE, 수량) 선택 $\rightarrow$ 장바구니 확인<br>• 가상 결제 및 중복 차단 요청 $\rightarrow$ 주문 번호 및 Guest Token 발급 $\rightarrow$ 제조 상태 기기 실시간 추적<br>• `접수 대기(PENDING)` 단계에서 변심 또는 실수 시 직접 주문 취소 |
| **Admin** | 매장 효율 운영,<br>품절 방지 및 보안 제어 | • 관리자 인증(로그인) $\rightarrow$ 매장 영업 시작/종료 토글 제어 $\rightarrow$ 실시간 재고 관리 및 품절 임박 경고 확인<br>• 신규 주문 인입 시 시각/청각 알림(Sound Alert) 확인 $\rightarrow$ 제조 상태 전이 처리<br>• 품절/변심 건에 대한 주문 취소 및 자동 재고 원복(Rollback) 트랜잭션 처리 $\rightarrow$ 메뉴 등록/수정/비활성화 |

---

## 3. 기능 요구사항 (Functional Requirements)

### 3.1 사용자 화면 (Customer UI)

#### `FR-C01` 메뉴 목록 조회 및 필터링
* **카테고리 필터링:** 카테고리별(커피, 논커피, 디저트 등) 메뉴 탭 및 필터링 기능 제공.
* **활성 메뉴 노출:** 관리자 의해 비활성화(`isActive: false`)된 메뉴는 고객 화면에서 즉시 숨김 처리.
* **메뉴 정보 노출:** 각 메뉴 카드에는 이미지, 메뉴명, 기본 가격, **남은 재고 수량** 표시.
* **품절 표시:** 재고가 `0`인 메뉴는 **[품절]** 뱃지 노출 및 클릭(선택) 비활성화 처리.
* **주기적 갱신:** 메뉴 목록 및 재고 상태는 SWR/Polling을 통해 주기적으로 최신 상태를 반영.

#### `FR-C02` 메뉴 옵션 선택 (모달 / 바텀시트)
* **옵션 선택:** 메뉴 클릭 시 모바일 바텀시트 또는 데스크톱 모달 호출. 온도 옵션 대상 메뉴(커피, 논커피)에 한하여 **HOT / ICE** 옵션 렌더링 (디저트류는 제외).
* **가격 정책:** v1.0 기준 HOT/ICE 옵션에 따른 추가 금액은 없음을 확정.
* **수량 검증:** 수량 조절 시 1 이상 현재 남은 재고 이하의 정수만 선택 가능하도록 차단하며, 초과 시 안내 메시지 표시.

#### `FR-C03` 장바구니 (Cart)
* **장바구니 관리:** 담은 메뉴명, 옵션(HOT/ICE), 수량, 단가 및 총 결제 금액 계산.
* **항목 병합 규칙:** 동일 메뉴라도 선택한 옵션(HOT vs ICE)이 다르면 서로 다른 개별 항목으로 분리하며, 동일 메뉴·동일 옵션일 경우에만 수량 합산 처리.
* **제어 기능:** 장바구니 내 수량 증감(재고 상한 검증), 개별 삭제 및 전체 비우기 기능 제공.
* **참고 정보 한계:** 장바구니의 데이터 및 가격은 참고용이며, 최종 결제 및 주문 생성 시 반드시 서버 측 트랜잭션을 통해 메뉴 유효성·최신 가격·재고를 재검증.

#### `FR-C04` 주문 및 가상 결제 (Mock Payment)
* **Mock 결제 시뮬레이션:** 결제 수단(신용카드, 카카오페이, 현금) 선택 후 주문 제출. v1.0에서는 실제 PG 승인 연동 없이 성공 처리되는 Mock 결제 시뮬레이션으로 규정.
* **멱등성 및 중복 방지:** 결제 버튼 연타 및 네트워크 재전송으로 인한 중복 주문/재고 차감 발생을 막기 위해 클라이언트에서 고유한 1회용 `requestId`(UUID)를 생성하여 동봉 (중복 시 기존 주문 반환 또는 무시).
* **트랜잭션 차감:** 주문 성공 시 데이터베이스 트랜잭션 내부에서 즉시 메뉴 재고 차감 및 주문 데이터 생성. (실패 시 장바구니 내역 유지)

#### `FR-C05` 실시간 주문 상태 확인 및 비회원 추적 (Guest Tracking)
* **비회원 식별자 보존:** 주문 성공 시 고객에게 노출되는 고유 주문 번호(`orderNo`, 예: `ord_20260801_001`) 및 식별용 `GuestToken`(또는 `publicToken`)을 발급하여 브라우저 `localStorage`에 자동 저장.
* **재진입 및 상태 복구:** 고객이 결제 후 브라우저 탭을 닫거나 이탈한 뒤 재입장하더라도, 로컬 스토리지의 토큰과 매칭되는 활성 주문(수령 전 상태)이 존재하는 경우 즉시 '주문 상태 확인' 페이지로 리다이렉트.
* **상태 전이 4단계:** 주문 진행 단계 실시간 안내 (`접수 대기` $\rightarrow$ `제조 중` $\rightarrow$ `제조 완료` $\rightarrow$ `수령 완료`). 취소 시 `주문 취소(CANCELLED)` 상태 별도 표시.
* **고객 주문 취소:** 주문 상태가 **`접수 대기(PENDING)`**인 경우에 한해, 고객이 직접 주문을 취소할 수 있는 **[주문 취소]** 버튼을 제공 (클릭 시 재고 자동 원복 및 상태 전환).

---

### 3.2 관리자 화면 (Admin UI)

#### `FR-A00` 관리자 인증 및 권한 제어 (Security - 신규)
* **접근 보안:** 재고 및 매장 조작 기능을 차단하기 위해, 관리자 화면(`/admin/**`)은 인증된 세션을 통해서만 접근 가능 (NextAuth/Auth.js 계정 로그인 또는 Supabase Auth / 미들웨어 PIN 인증 기반).
* **서버사이드 검증:** 클라이언트 접근 허가와 별개로, 관리자용 Server Action 실행 시 매 요청마다 서버에서 관리자 권한 및 세션 유효성을 다시 검증. 비인가 접근 시 로그인 화면으로 리다이렉트.

#### `FR-A01` 매장 영업 상태 제어 (Store Status - 신규)
* **글로벌 영업 제어:** 매장 전역 온/오프라인 상태 조작을 위한 **[영업 중 / 주문 마감]** 토글 스위치 제공.
* **주문 전면 차단:** 매장이 바쁘거나 영업 닫힘(`isOpen: false`) 상태로 전환 시, 사용자 화면(Customer UI)에 영업 중단 알림 배너를 표출하고 장바구니 결제 및 신규 주문 진입을 전면 차단.

#### `FR-A02` 실시간 재고 관리 (핵심 요구사항)
* **재고 대시보드:** 전체 메뉴(활성/비활성 포함)의 남은 재고(Stock)를 확인할 수 있는 관리 테이블/카드 제공.
* **수량 제어:** 메뉴별 재고 증감(`+`, `-`) 버튼 및 직접 숫자 입력 수정 기능 지원. (음수/소수점 입력 차단, 0 이상의 정수만 검증 허용)
* **품절 임박 경고:** 재고 수량이 임계값(기본값 3개 이하)에 도달하면 행 또는 숫자에 시각적 경고 색상 렌더링.
* **즉시 반응:** 재고를 `0`으로 설정 시 즉시 품절 상태로 변경되며, 사용자 화면에 준실시간(최대 5초 이내) 반영.

#### `FR-A03` 실시간 주문 수주, 알림 및 상태 제어
* **주문 대기열 조회:** 인입된 주문 건을 최신순 카드/리스트 형태로 렌더링 (주문 번호, 시간, 메뉴·옵션 스냅샷 내역, 결제 수단, 총금액 표기).
* **청각 및 시각 알림 (Sound Alert):** 주방/카운터 환경 특성을 반영하여 새로고침 없이(SWR/Polling 또는 SSE) 신규 주문 데이터가 인입되면, 상단 Toast 팝업 배지와 함께 **청각적 알림(Audio 객체 재생 / 비프 음성 알림)** 발생.
* **상태 전이 처리:** 현재 주문 상태에 부합하는 상태 변경 버튼 제공:
  * `접수 대기(PENDING)` $\rightarrow$ **[주문 승인/제조 시작]** $\rightarrow$ `제조 중(PREPARING)`
  * `제조 중(PREPARING)` $\rightarrow$ **[제조 완료]** $\rightarrow$ `제조 완료(COMPLETED)`
  * `제조 완료(COMPLETED)` $\rightarrow$ **[수령 완료]** $\rightarrow$ `수령 완료(PICKED_UP)`

#### `FR-A04` 주문 취소 및 재고 자동 원복
* **관리자 취소 권한:** 관리자는 `접수 대기(PENDING)` 및 `제조 중(PREPARING)` 단계에서 주문 취소가 가능 (`COMPLETED`, `PICKED_UP` 단계는 불필요한 클레임 및 정산 왜곡 방지를 위해 취소 제한).
* **트랜잭션 재고 원복(Rollback):** 주문 취소 시 주문 상태를 `CANCELLED`로 변경함과 동시에, 해당 주문에 포함되었던 각 메뉴 항목의 수량만큼 DB 재고를 정확히 복구하는 원자적 트랜잭션 실행.
* **중복 롤백 방지:** 이미 `CANCELLED` 처리된 주문은 재차 취소하거나 재고를 중복으로 복구하지 않도록 차단. (v1.0에서는 단건 전체 취소만 지원하며 항목 단위 부분 취소는 미지원)

#### `FR-A05` 메뉴 관리 (Menu CRUD - 신규)
* **메뉴 관리 기능:** 신규 메뉴 등록, 메뉴명·가격·카테고리·초기 재고(`initialStock`)·이미지 경로 수정 지원.
* **판매 중단 및 무결성 보존:** 기존 주문 데이터의 참조 무결성을 지키기 위해, 주문 이력이 존재하는 메뉴는 DB에서 하드 삭제(Hard Delete)하지 않고, 판매 중단 플래그(`isActive: false`) 토글을 통해 화면 노출 여부만 변경.

#### `FR-A06` 시스템 데이터 초기화 (개발/테스트용)
* **리셋 기능 분리:** 오버되거나 꼬인 테스트 상태를 가라앉히기 위해 기능 분리 제공:
  1. **[재고 초기화]:** 전체 메뉴의 재고 수량을 설정된 `initialStock` 기준값으로 즉시 원복.
  2. **[주문 내역 비우기]:** 쌓인 테스트 주문 이력 일괄 삭제.
* **운영 환경 보안 차단:** Production 운영 배포 환경에서는 초기화 기능에 접근하지 못하도록 화면 비활성화 또는 Server Action 내 환경 변수(ENV) 검증 차단.

---

## 4. 예외 처리 시나리오 (Edge Cases & Exception Handling)

1. **동시성 이슈 (장바구니 담기 후 결제 직전 품절 발생):**
   * *상황:* 사용자가 장바구니에 상품을 담아둔 사이 관리자가 해당 메뉴 재고를 `0`으로 수정하거나, 타 사용자가 마지막 재고를 선점 결제한 경우.
   * *대응:* 결제 요청 시 Server Action에서 트랜잭션 진입 직후 최신 DB 재고를 조건절로 검증. 수량이 부족한 경우 트랜잭션을 롤백하고, *"죄송합니다. [메뉴명] 상품이 방금 품절( 또는 잔여 N개) 되었습니다."* 오류를 반환하여 장바구니를 비우지 않고 Toast 경고 안내.
2. **동시에 마지막 남은 재고 쟁탈 주문:**
   * *상황:* 재고가 1개 남은 아이템을 두 명의 고객이 밀리초 차이로 동시에 결제 승인을 시도하는 경우.
   * *대응:* DB 레코드 Row Lock 또는 조건부 업데이트(`where: { id, stock: { gte: quantity } }`)를 적용. 원자적 차감에 먼저 성공한 1건만 주문 생성되며, 실패한 요청은 품절 에러 반환(음수 재고 원천 금지).
3. **중복 결제 요청 및 네트워크 재시도 방지:**
   * *상황:* 사용자의 브라우저 지연 등으로 결제 버튼을 연타하거나, 통신 불안정으로 HTTP 요청이 재전송되는 경우.
   * *대응:* 요청 폼에 포함된 고유 멱등성 키(`requestId`)로 DB `unique` 인덱스 충돌 검증. 동일한 ID로 도달한 중복 요청은 추가 차감 없이 최초 처리된 주문 내역만 돌려보내거나 무시하며, 결제 진행 중엔 버튼 비활성화.
4. **사용자 이탈 후 재진입 시 주문 상태 자동 복구:**
   * *상황:* 비로그인 고객이 모바일로 주문 완료 후 실수로 웹 창(탭)을 닫았다가 10분 뒤 웹앱에 재접속한 경우.
   * *대응:* 기기 로컬 스토리지에 저장된 최근 `GuestToken`과 주문번호를 기반으로 DB를 확인하여, 아직 수령 완료(`PICKED_UP`) 혹은 취소(`CANCELLED`)되지 않은 활성 주문이 있으면 메인 화면 대신 `/order/[orderNo]` 추적 페이지로 자동 리다이렉트.
5. **장바구니 메뉴 관리자 판매 중단(비활성화) 대응:**
   * *상황:* 고객이 장바구니에 상품을 담아둔 상태에서 관리자가 해당 메뉴를 판매 중단(`isActive = false`) 처리한 경우.
   * *대응:* 주문 생성 시 활성 플래그를 서버 검증하여, 비활성 아이템이 포함된 경우 결제를 차단하고 장바구니에서 해당 메뉴를 제외하도록 메시지 고지.
6. **관리자 재고/수량 비정상 입력 검증:**
   * *상황:* 관리자 화면 또는 고객 옵션 모달에서 음수, 소수점, 극단적으로 큰 문자열 수량 값이 전달되는 경우.
   * *대응:* 클라이언트 UI 제한뿐 아니라 Server Action에서 Zod 등을 사용하여 0 이상의 양의 정수만 허용하도록 철저히 입력을 스케일링·검증.
7. **주문 취소 버튼 중복 클릭 또는 멀티 탭 취소:**
   * *상황:* 관리자나 고객이 주문 취소를 연속으로 누르거나 다수 단말기에서 동시에 취소를 시도해 재고가 다중 원복될 우려가 있는 경우.
   * *대응:* DB 트랜잭션 조건문(`status: { in: ["PENDING", "PREPARING"] }`)으로 상태 전환에 성공했을 때만 재고 증가 로직을 1회 실행하여 완벽한 멱등성 보장.

---

## 5. 핵심 데이터 모델 (Data Entities & Snapshots)

* **정합성 원칙:** 주문 내역(`OrderItem`)은 메뉴의 단가나 이름이 이후 변경되더라도 과거 거래 기록이 왜곡되지 않도록, **주문 생성 시점의 메뉴명, 옵션, 단가 스냅샷**을 영구 보존합니다.
* **타임존 규격:** DB의 생성/수정 시각은 국제 표준 **UTC(`Z`)**로 저장하며, 프론트엔드 UI(고객·관리자)에서는 KST(**`Asia/Seoul`**) 단위로 변환하여 시간대를 출력합니다.

```
[ StoreSettings ] (1)  ── 매장 영업 온/오프 전역 설정

[ Menu ] (1) ──── (N) [ OrderItem (Snapshot) ] (N) ──── (1) [ Order ]
```

### JSON 데이터 구조 예시

```json
// StoreSettings (매장 영업 상태)
{
  "id": 1,
  "isOpen": true
}

// Menu (메뉴 목록 및 재고 데이터)
{
  "id": "cuid_menu_01",
  "name": "아메리카노",
  "price": 4000,
  "category": "COFFEE",
  "stock": 12,
  "initialStock": 15,
  "lowStockThreshold": 3,
  "isActive": true,
  "imageUrl": "https://storage.example.com/images/americano.png",
  "createdAt": "2026-08-01T00:00:00Z"
}

// Order (주문 정보 및 스냅샷 데이터)
{
  "id": "cuid_order_102938",
  "orderNo": "ord_20260801_001",
  "guestToken": "gt_8F3mZpL2sR7x_random",
  "requestId": "uuid-client-req-001",
  "status": "PREPARING", // PENDING, PREPARING, COMPLETED, PICKED_UP, CANCELLED
  "paymentMethod": "KAKAO_PAY",
  "totalAmount": 8000,
  "createdAt": "2026-08-01T06:50:00Z", // DB UTC 저장 (KST 기준 15:50:00)
  "items": [
    {
      "id": "cuid_item_99812",
      "menuId": "cuid_menu_01",
      "menuName": "아메리카노",      // 주문 당시 스냅샷
      "option": "ICE",             // "HOT" | "ICE" 옵션 스냅샷
      "quantity": 2,
      "unitPrice": 4000,           // 주문 당시 단가 스냅샷
      "lineTotalAmount": 8000
    }
  ]
}
```

---

## 6. 비기능 요구사항 (Non-Functional Requirements)

1. **반응형 레이아웃 및 최적화:**
   * **사용자 화면 (Customer):** 모바일 브라우저(375px~) 및 터치 조작 최적화 (옵션 선택 및 장바구니는 모바일 바텀시트 UI 우선 적용).
   * **관리자 화면 (Admin):** 데스크톱 및 태블릿 포스기 화면(768px~)에 맞춰 가시성 높게 최적화. (작은 화면 조회 시 가로 스크롤 및 카드 그리드 적응)
2. **준실시간 상태 동기화 (Near Real-Time Synchronization):**
   * v1.0에서의 실시간성은 서버 데이터 변경 후 **최대 3~5초 이내**에 타 사용자의 화면에 자동 동기화되는 준실시간(Near Real-Time) 수준으로 규정.
   * 사용자 메뉴 목록(5초 Polling/SWR), 관리자 주문 대기열 및 고객 주문 상태(3초 Polling/SWR)를 적용하며, 향후 SSE(Server-Sent Events), Supabase Realtime 등 Push 기반 웹소켓/스트리밍으로 유연히 전환 가능한 아키텍처로 구성.
3. **데이터 정합성 및 무결성:**
   * 주문 생성 및 취소 시 수량 차감·복원 연산은 어떠한 외부 예외나 통신 유실에도 분리되지 않도록 반드시 1개의 DB 트랜잭션 범위 안에서 완수.
   * 클라이언트에서 넘어온 금액 총합이나 재고 잔량을 과신하지 않고, 서버에서 최신 메뉴 단가와 재고를 다시 조회한 후 총금액을 재계산하여 조작 변조를 원천 봉쇄.
4. **보안 및 환경 호환 (Deployment Infrastructure):**
   * **로컬 개발 / 프로토타입:** 간편한 구동을 위해 SQLite 또는 로컬 PostgreSQL 사용 가능.
   * **스테이징 / 운영 (Production) 배포:** Vercel 등 서버리스(Serverless) 호스팅 환경의 파일시스템 read-only 및 인스턴스 휘발 제약을 해소하기 위해, **PostgreSQL (Supabase, Neon 또는 관리형 DB)** 연동을 운영 인프라의 확정 스펙으로 강제.
5. **접근성 및 오류 가시화:**
   * 대화상자(Dialog/Modal) 포커스 트랩, 모든 작동 버튼의 `aria-label` 부여 및 명확한 WCAG 기준 명도 대비를 유지.
   * 장애 또는 경계 시나리오(재고 없음, 권한 오류, 통신 장애 등) 발생 시 모던한 Toast 알림 및 명확한 한국어 안내 메시지 렌더링을 제공하여 UX 저하를 방지.
